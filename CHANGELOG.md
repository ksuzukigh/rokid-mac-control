# 変更履歴

## 1.2.1 - 未リリース

- メモ・Home・アプリ一覧を`M`・`H`・`A`で直接開く操作へ変更
- 矢印キーで下段アイコンを選ぶ方式（水色の丸い印）を廃止。Rokid本体の画面とMac側の選択状態が食い違い、画面によってキーの意味が変わっていたため
- 矢印キーと`Enter`は`A`でアプリ一覧を開いてから有効へ変更。アプリを開いて`Esc`で一覧へ戻ってもそのまま選び直せる
- Rokid画面の上へ操作案内を常時表示。アプリ一覧を選んでいる間だけ矢印キーの案内へ切り替え
- 10秒ごとに送っていた`KEYCODE_WAKEUP`と`KEYCODE_UNKNOWN`を廃止。定期的に鳴っていた音の原因を解消
- Rokid本体の設定を書き換えない方針へ変更。RV101では`screen_off_timeout`がHUDの表示を左右せず、変更しなくてもMac操作を続けられることを実機で確認したため
- 監視スクリプトは、設定を変更していた版が残した値を終了時に元へ戻す
- `Space`の中央タップ・中央ダブルタップを廃止。マウス操作で代用できるため
- キー操作の判定をADB処理から分離し、模擬ADBによる自動試験を追加
- `adb tcpip 5555`を廃止。USB接続から無線へ移る際は、Android標準のワイヤレスデバッグ（暗号化）を有効にして接続する
- 起動時・再接続時のどちらでUSBを見つけた場合も、暗号化された無線接続を用意し直して無線へ戻る
- 接続先の候補を、直前・保存済み・`adb devices`の接続済み端末・mDNS探索の順に統一。重複を除き、固定5555番はどの経路からも除外する
- 接続先を採用・保存する前に、端末に暗号化されていない接続の入口が残っていないことを確認するよう変更。TLSで接続していても、別のポートに入口が開いていれば同じWi-Fi内の別の機器から接続できてしまうため
- USB接続時は、ポート番号によらず暗号化されていない入口を閉じ、実際に閉じたことを確認してから無線へ移行する。閉じられたことを確認できない場合は起動を中止する
- 端末の状態を読み取れない場合は、安全とみなさず接続先を採用しない
- 暗号化されていない入口を閉じられたか確認できない場合は、起動を中止して画面に理由と対処を表示する。安全を確認できない状態のまま気づかずに使い続けることがないようにする
- 過去に保存された固定5555番の接続先は再利用せず破棄。暗号化されない接続へ戻らないようにする
- 暗号化された接続を用意できない場合は、暗号化なしへ落とさずUSB接続を継続する
- 接続のキャンセルを実行中のADB処理へ伝えるよう変更。長いADB待機の後ろで終了処理が詰まらなくなり、時間切れと利用者キャンセルを別の結果として扱う

## 1.2.0 - 2026-07-28

- `Shift` + `←`で開いていた下段のメモは、`↓`→`←`→`Enter`で開く操作へ変更
- ホーム画面の上下段と下段3項目を矢印キーだけで移動できるよう改善
- 接続先がRokidであることを確認してから操作を開始
- ⌘Qと再接続回数の上限を追加
- 接続待ちと終了処理の進捗表示、接続キャンセルを追加
- 終了処理、映像合成、キーボード操作の安定性を改善
- Rokid画面とほかのMacアプリを正常に切り替えられるよう改善
- ログ、セルフテスト、CI、ライセンス表記を整備

## 1.1.0 - 2026-07-27

- ライブ映像と背景なし（省電力）を正式版へ統合
- ライブ映像へRokidの文字・アイコンを重ねる表示を追加
- 文字・アイコンの見やすさ調整を追加
- ADBとscrcpyをアプリ内へ同梱し、Homebrew依存を解消
