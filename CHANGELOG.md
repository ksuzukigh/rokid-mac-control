# 変更履歴

## 1.2.6 - 2026-08-16

- `M メモ　H Home　A アプリ`などの操作案内をライブ映像上の重ね表示からウインドウ上部の専用欄へ移動
- 操作案内と見やすさ調整を同じ1段の上部操作欄へまとめ、切れや重なりを防止
- 操作案内を青いバッジで強調し、見やすさ調整スライダーを長くして操作性を改善
- 青い`M メモ`・`H Home`・`A アプリ`をクリック可能なボタンに変更（キーボード操作も従来どおり利用可能）
- 青いボタンをコンパクトにしてスライダーをさらに延長し、境界線を明確に右へ移動
- 操作案内がライブ映像を隠さず、アプリ一覧選択中の案内切替も維持

## 1.2.5 - 2026-08-16

- 「ライブ映像」使用中にRokid ZOOM IN CAMERAを開いたとき、背景用カメラを自動停止し、倍率・操作案内・録画時間を含む撮影画面をMacへ表示
- Rokid ZOOM IN CAMERAを閉じた後、元のライブ映像へ自動復帰
- 純正カメラとRokid ZOOM IN CAMERAを同じカメラ画面切替で安全に扱う自動試験を追加

## 1.2.4 - 2026-07-31

- Macの別アプリからRokid画面へ戻るためにクリックしたとき、アプリ一覧の選択状態が解除され、左右キーが反応しなくなる問題を修正
- Rokid画面と同じプロセスにある小さな補助ウインドウを操作案内の位置合わせ対象と誤認し、案内が左へずれる場合がある問題を修正
- Photo to Mac、Rokid純正「カメラ」、テンプル右上の撮影ボタンを、「ライブ映像」と「背景なし（省電力）」の両方で実機確認し、表示の違いと写真の保存先をREADMEへ追記

## 1.2.3 - 2026-07-31

- ライブ映像中に純正「カメラ」を開いた場合、Rokid Controlを終了せず、純正カメラのカラー撮影画面へ自動で切り替える機能を追加
- 純正カメラを閉じると、背面カメラへ文字・アイコンを重ねたライブ映像へ自動復帰
- 純正カメラの履歴ではなく、現在最前面のアプリを確認して切り替えるため、ほかのアプリを純正カメラと誤判定しない
- 純正カメラ使用中にカメラ受信の再試行を繰り返してRokid Controlが終了する問題を修正
- カメラ映像の切替直後にmacOSが終了済みの受信処理へキーを送ろうとし、`A`・`M`・`H`が反応しない場合がある問題を修正
- `A`・`M`・`H`はRokidのHome画面が実際に前面へ戻ったことを確認してから下段を開き、画面切替中のタップ取りこぼしを防止

## 1.2.2 - 2026-07-31

- Rokid Control専用のADBサーバーを標準のADBから分離し、起動のたびに作り直すよう変更。古い開発版が残したmacOSのローカルネットワーク許可状態を翌日以降も再利用し、Wi-Fi接続だけが拒否される問題を修正
- Rokid Control終了時に専用ADBサーバーも終了し、次回起動へ古い接続状態を残さない
- macOSからローカルネットワーク通信を拒否された場合、「Wi-Fi ON」やケーブル接続ではなく、ローカルネットワーク許可を一度オフ・オンする正しい対処を表示
- READMEへ、設定がオンに見えても内部許可が食い違う場合の復旧手順を追加

## 1.2.1 - 2026-07-30

- メモ・Home・アプリ一覧を`M`・`H`・`A`で直接開く操作へ変更
- 矢印キーで下段アイコンを選ぶ方式（水色の丸い印）を廃止。Rokid本体の画面とMac側の選択状態が食い違い、画面によってキーの意味が変わっていたため
- 左右キーと`Enter`は`A`でアプリ一覧を開いてから有効へ変更。アプリを開いて`Esc`で一覧へ戻ってもそのまま選び直せる
- Rokid画面の上へ操作案内を常時表示。アプリ一覧を選んでいる間だけ左右キーの案内へ切り替え
- 実機では使わない上下キーの割り当てを削除し、README・コード・テストを左右キーだけの仕様へ統一
- 5秒以上操作しないとRokidがスリープして左右キーを無視する問題を修正。Mac操作中だけ画面消灯までの時間を一時的に延長し、正常終了・端末側監視・次回起動の3経路で元の値へ戻す
- 10秒ごとに送っていた`KEYCODE_WAKEUP`と`KEYCODE_UNKNOWN`を廃止。定期的に鳴っていた音の原因を解消
- 画面消灯設定は開始前に保存し、変更後と復元後の値も確認。安全に保存・確認できない場合はMac操作を開始しない
- 監視スクリプトは、設定を変更していた版が残した値を終了時に元へ戻す
- `Space`の中央タップ・中央ダブルタップを廃止。マウス操作で代用できるため
- キー操作の判定をADB処理から分離し、模擬ADBによる自動試験を追加
- `adb tcpip 5555`を廃止。USB接続から無線へ移る際は、Android標準のワイヤレスデバッグ（暗号化）を有効にして接続する
- 起動時・再接続時のどちらでUSBを見つけた場合も、暗号化された無線接続を用意し直して無線へ戻る
- 接続先の候補を、直前・保存済み・`adb devices`の接続済み端末・mDNS探索の順に統一。重複を除き、固定5555番はどの経路からも除外する
- 接続先を採用・保存する前に、端末に暗号化されていない接続の入口が残っていないことを確認するよう変更。TLSで接続していても、別のポートに入口が開いていれば同じWi-Fi内の別の機器から接続できてしまうため
- USB接続時は、ポート番号によらず暗号化されていない入口を閉じ、実際に閉じたことを確認してから無線へ移行する。閉じられたことを確認できない場合は起動を中止する
- mDNSで暗号化接続先を発見できない場合は、USB接続中のRokidが申告するWi-FiアドレスとTLSポートを予備候補として使用する。接続後のRokid確認と平文入口がないことの確認は同じように行う
- macOSのローカルネットワーク許可を追加し、同じWi-Fi上のRokidとBonjourの暗号化接続先を検索できるようにする
- 端末の状態を読み取れない場合は、安全とみなさず接続先を採用しない
- 暗号化されていない入口を閉じられたか確認できない場合は、起動を中止して画面に理由と対処を表示する。安全を確認できない状態のまま気づかずに使い続けることがないようにする
- 過去に保存された固定5555番の接続先は再利用せず破棄。暗号化されない接続へ戻らないようにする
- 暗号化された接続を用意できない場合は、暗号化なしへ落とさずUSB接続を継続する
- 接続のキャンセルを実行中のADB処理へ伝えるよう変更。長いADB待機の後ろで終了処理が詰まらなくなり、時間切れと利用者キャンセルを別の結果として扱う

## 1.2.0 - 2026-07-28

- `Shift` + `←`で開いていた下段のメモは、`↓`→`←`→`Enter`で開く操作へ変更
- ホーム画面の上下段と下段3項目を矢印キーだけで移動できるよう改善
- 接続先がRokidであることを確認してから操作を開始
- ⌘Qと再接続回数の上限を追加
- 接続待ちと終了処理の進捗表示、接続キャンセルを追加
- 終了処理、映像合成、キーボード操作の安定性を改善
- Rokid画面とほかのMacアプリを正常に切り替えられるよう改善
- ログ、セルフテスト、CI、ライセンス表記を整備

## 1.1.0 - 2026-07-27

- ライブ映像と背景なし（省電力）を正式版へ統合
- ライブ映像へRokidの文字・アイコンを重ねる表示を追加
- 文字・アイコンの見やすさ調整を追加
- ADBとscrcpyをアプリ内へ同梱し、Homebrew依存を解消
